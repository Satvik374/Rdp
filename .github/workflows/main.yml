name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          netsh advfirewall firewall delete rule name="RDP-Tunnel"
          netsh advfirewall firewall add rule name="RDP-Tunnel" `
            dir=in action=allow protocol=TCP localport=3389

          Restart-Service -Name TermService -Force
          Write-Host "RDP configured successfully"

      - name: Create RDP User with Secure Password
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) +
                         [char[]](91..96) + [char[]](123..126))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"

          echo "RDP_PASSWORD=$password" >> $env:GITHUB_ENV

          if (-not (Get-LocalUser -Name "RDP")) {
              Write-Error "User creation failed"
              exit 1
          }
          Write-Host "RDP user created successfully"

      - name: Download and Install LocaltoNet
        shell: powershell
        run: |
          Write-Host "Downloading LocaltoNet..."
          Invoke-WebRequest `
            -Uri "https://localtonet.com/download/localtonet-windows-x64.zip" `
            -OutFile "localtonet.zip"

          Write-Host "Extracting..."
          Expand-Archive -Path "localtonet.zip" -DestinationPath "C:\localtonet" -Force

          Write-Host "Files extracted:"
          Get-ChildItem "C:\localtonet"

          # Add to PATH
          $env:PATH += ";C:\localtonet"

          # Verify
          if (Test-Path "C:\localtonet\localtonet.exe") {
              Write-Host "LocaltoNet installed successfully"
              & "C:\localtonet\localtonet.exe" --version
          } else {
              Write-Error "localtonet.exe not found after extraction"
              exit 1
          }

      - name: Start LocaltoNet Tunnel
        shell: powershell
        run: |
          # Authenticate and start tunnel in background
          $process = Start-Process -FilePath "C:\localtonet\localtonet.exe" `
            -ArgumentList "--authtoken ${{ secrets.LOCALTONET_TOKEN }}" `
            -WindowStyle Hidden `
            -RedirectStandardOutput "C:\ltn-log.txt" `
            -RedirectStandardError "C:\ltn-err.txt" `
            -PassThru

          Write-Host "LocaltoNet PID: $($process.Id)"

          $connected = $false
          $retries = 0
          $maxRetries = 30

          while (-not $connected -and $retries -lt $maxRetries) {
              Start-Sleep -Seconds 5
              $retries++
              Write-Host "Attempt $retries/$maxRetries - Waiting for tunnel..."

              if ($process.HasExited) {
                  Write-Host "--- STDOUT ---"
                  if (Test-Path "C:\ltn-log.txt") { Get-Content "C:\ltn-log.txt" }
                  Write-Host "--- STDERR ---"
                  if (Test-Path "C:\ltn-err.txt") { Get-Content "C:\ltn-err.txt" }
                  Write-Error "LocaltoNet exited unexpectedly"
                  exit 1
              }

              $logFiles = @("C:\ltn-log.txt", "C:\ltn-err.txt")
              foreach ($logFile in $logFiles) {
                  if (Test-Path $logFile) {
                      $log = Get-Content $logFile -Raw
                      Write-Host "--- Log ---"
                      Write-Host $log

                      # Check for successful tunnel connection
                      if ($log -match "tunnel|connected|started|listening|forwarding|ready") {
                          $connected = $true
                          Write-Host "Tunnel is active!"
                          break
                      }
                  }
              }
          }

          if (-not $connected) {
              Write-Host "--- Final STDOUT ---"
              if (Test-Path "C:\ltn-log.txt") { Get-Content "C:\ltn-log.txt" }
              Write-Host "--- Final STDERR ---"
              if (Test-Path "C:\ltn-err.txt") { Get-Content "C:\ltn-err.txt" }
              Write-Host "Warning: Could not confirm tunnel - it may still be running"
          }

      - name: Maintain Connection
        shell: powershell
        run: |
          Write-Host "`n=== RDP ACCESS DETAILS ==="
          Write-Host "Address:  ${{ secrets.LOCALTONET_SERVER }}"
          Write-Host "Port:     ${{ secrets.LOCALTONET_REMOTE_PORT }}"
          Write-Host "Username: RDP"
          Write-Host "Password: $env:RDP_PASSWORD"
          Write-Host "Connect:  ${{ secrets.LOCALTONET_SERVER }}:${{ secrets.LOCALTONET_REMOTE_PORT }}"
          Write-Host "=========================="

          while ($true) {
              Write-Host "[$(Get-Date)] RDP Active - Cancel workflow to terminate"
              Start-Sleep -Seconds 300
          }
