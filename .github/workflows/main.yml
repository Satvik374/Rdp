name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          netsh advfirewall firewall delete rule name="RDP-Serveo"
          netsh advfirewall firewall add rule name="RDP-Serveo" `
            dir=in action=allow protocol=TCP localport=3389

          Restart-Service -Name TermService -Force
          Write-Host "RDP configured successfully"

      - name: Create RDP User with Secure Password
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) +
                         [char[]](91..96) + [char[]](123..126))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"

          echo "RDP_PASSWORD=$password" >> $env:GITHUB_ENV

          if (-not (Get-LocalUser -Name "RDP")) {
              Write-Error "User creation failed"
              exit 1
          }
          Write-Host "RDP user created successfully"

      - name: Enable OpenSSH Client
        run: |
          Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0
          Write-Host "OpenSSH client ready"

      - name: Start Serveo Tunnel
        run: |
          # Generate SSH key silently
          ssh-keygen -t rsa -b 2048 -f "$env:USERPROFILE\.ssh\id_rsa" -N "" -q

          # Start SSH reverse tunnel to serveo.net
          $process = Start-Process -FilePath "ssh" `
            -ArgumentList "-o StrictHostKeyChecking=no -o ServerAliveInterval=30 -o ServerAliveCountMax=10 -R 0:localhost:3389 serveo.net" `
            -WindowStyle Hidden `
            -RedirectStandardOutput "C:\serveo-log.txt" `
            -RedirectStandardError "C:\serveo-err.txt" `
            -PassThru

          Write-Host "SSH Tunnel PID: $($process.Id)"

          $port = $null
          $retries = 0
          $maxRetries = 30

          while (-not $port -and $retries -lt $maxRetries) {
              Start-Sleep -Seconds 5
              $retries++
              Write-Host "Attempt $retries/$maxRetries - Waiting for tunnel..."

              if ($process.HasExited) {
                  Write-Host "--- Serveo STDOUT ---"
                  if (Test-Path "C:\serveo-log.txt") { Get-Content "C:\serveo-log.txt" }
                  Write-Host "--- Serveo STDERR ---"
                  if (Test-Path "C:\serveo-err.txt") { Get-Content "C:\serveo-err.txt" }
                  Write-Error "SSH tunnel exited unexpectedly"
                  exit 1
              }

              $logFiles = @("C:\serveo-log.txt", "C:\serveo-err.txt")
              foreach ($logFile in $logFiles) {
                  if (Test-Path $logFile) {
                      $logContent = Get-Content $logFile -Raw
                      Write-Host "--- Log ---"
                      Write-Host $logContent

                      # Serveo prints: Allocated port 12345 for remote forward
                      if ($logContent -match "Allocated port (\d+) for remote forward") {
                          $port = $matches[1]
                          Write-Host "Port allocated: $port"
                          break
                      }
                  }
              }
          }

          if (-not $port) {
              Write-Host "--- Final STDOUT ---"
              if (Test-Path "C:\serveo-log.txt") { Get-Content "C:\serveo-log.txt" }
              Write-Host "--- Final STDERR ---"
              if (Test-Path "C:\serveo-err.txt") { Get-Content "C:\serveo-err.txt" }
              Write-Error "Serveo tunnel not established after $maxRetries attempts."
              exit 1
          }

          echo "SERVEO_PORT=$port" >> $env:GITHUB_ENV

      - name: Maintain Connection
        run: |
          Write-Host "`n=== RDP ACCESS DETAILS ==="
          Write-Host "Address:  serveo.net"
          Write-Host "Port:     $env:SERVEO_PORT"
          Write-Host "Username: RDP"
          Write-Host "Password: $env:RDP_PASSWORD"
          Write-Host "Connect:  serveo.net:$env:SERVEO_PORT"
          Write-Host "=========================="

          while ($true) {
              Write-Host "[$(Get-Date)] RDP Active - Cancel workflow to terminate"
              Start-Sleep -Seconds 300
          }
